using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using WEA.Core.Entities;
using WEA.SharedKernel.Interfaces;

namespace WEA.Core
{
    public static class DatabasePopulator
    {
      /*  public static int Populate(IRepository repository)
        {
            if (repository.GetAll<TestEntity>().Count() >= 3) return 0;

            repository.AddAsync(new TestEntity
            {
                Title = "Get Sample Working",
                Description = "Try to get the sample to build."
            }).Wait();
            repository.AddAsync(new TestEntity
            {
                Title = "Review Solution",
                Description = "Review the different projects in the solution and how they relate to one another."
            }).Wait();
            repository.AddAsync(new TestEntity
            {
                Title = "Run and Review Tests",
                Description = "Make sure all the tests run and review what they are doing."
            }).Wait();

            return repository.GetAll<TestEntity>().Count();
        }*/
    }
}
